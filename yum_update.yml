# file:  yum_update.yml (playbook)
---
#- hosts: '{{ target }}'
- hosts: all
  sudo: yes
  remote_user: chartrse
  tasks:
   - name: upgrade all packages
     yum: name=* state=latest
     async: 7200
     poll: 30
   #- name: Restart server
    # shell: " sleep 3; reboot"
    # async: 1
    # poll: 0

